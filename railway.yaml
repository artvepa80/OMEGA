build:
  builder: DOCKERFILE
  dockerfile: Dockerfile.railway

deploy:
  startCommand: python3 main.py --ai-mode --enable-performance-monitoring --port $PORT
  healthcheckPath: /health
  healthcheckTimeout: 300