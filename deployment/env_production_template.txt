# Configuración de producción para Omega
# Copia este archivo como .env.production y completa los valores

# ============ BASE DE DATOS ============
DATABASE_URL=postgresql://username:password@postgres:5432/omega_db
POSTGRES_USER=omega_user
POSTGRES_PASSWORD=tu_password_super_seguro

# ============ SEGURIDAD ============
JWT_SECRET=tu_jwt_secret_muy_seguro_256_bits
API_SECRET_KEY=tu_api_secret_key_seguro

# ============ CORS Y DOMINIOS ============
CORS_ORIGINS=https://tudominio.com,https://app.tudominio.com
ALLOWED_HOSTS=tudominio.com,app.tudominio.com

# ============ CONFIGURACIÓN DE LOGS ============
LOG_LEVEL=INFO
SENTRY_DSN=https://tu_sentry_dsn_aqui

# ============ SERVICIOS EXTERNOS ============
# WhatsApp (opcional)
WHATSAPP_TOKEN=tu_whatsapp_token
WHATSAPP_PHONE_ID=tu_phone_number_id

# Twilio (opcional)
TWILIO_ACCOUNT_SID=tu_twilio_sid
TWILIO_AUTH_TOKEN=tu_twilio_token

# ============ NOTIFICACIONES ============
# Para iOS Push Notifications
APNS_KEY_ID=tu_apns_key_id
APNS_TEAM_ID=tu_team_id
APNS_BUNDLE_ID=com.tuempresa.OmegaApp

# Email (opcional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=tu_email@gmail.com
SMTP_PASSWORD=tu_app_password

# ============ MONITORING ============
GRAFANA_PASSWORD=tu_grafana_password

# ============ ALMACENAMIENTO ============
# AWS S3 (para modelos ML)
AWS_ACCESS_KEY_ID=tu_aws_access_key
AWS_SECRET_ACCESS_KEY=tu_aws_secret_key
AWS_S3_BUCKET=tu-omega-models-bucket
AWS_REGION=us-east-1

# ============ CONFIGURACIÓN DE APLICACIÓN ============
ENVIRONMENT=production
DEBUG=false
WORKERS=4
MAX_REQUESTS=1000
TIMEOUT=30

# ============ RATE LIMITING ============
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_BURST=10
